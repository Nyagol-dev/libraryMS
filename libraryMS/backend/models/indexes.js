/**
 * createIndexes — stub for database index definitions.
 * Will be filled in a later step with compound/sparse indexes
 * for performance-critical queries (e.g. transactions, books).
 */
const createIndexes = async () => {
  console.log('createIndexes: (stub) no indexes defined yet');
};

module.exports = { createIndexes };
